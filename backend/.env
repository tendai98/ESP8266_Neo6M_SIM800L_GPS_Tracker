# Firebase RTDB URL (from Firebase console)
FB_DB_URL=https://gps-tracking-system-fe24e-default-rtdb.firebaseio.com

# TCP ingest listener
TCP_HOST=127.0.0.1
TCP_PORT=9331

# HTTP API
HTTP_HOST=127.0.0.1
HTTP_PORT=8080

# Optional admin API key for write endpoints (POST /api/devices, etc.)
API_KEY=49c148a82e27e198e687bc1d68fcc674

# Optional telemetry retention (days); leave empty to disable cleanup job
RETENTION_DAYS=180

# Path to service account key (or set GOOGLE_APPLICATION_CREDENTIALS externally)
# GOOGLE_APPLICATION_CREDENTIALS=serviceAcc.json
